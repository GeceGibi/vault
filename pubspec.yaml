name: keep
description: "Typed, reactive, encrypted local storage for Flutter."
version: 0.4.0
homepage: https://github.com/GeceGibi/keep

topics:
  - storage
  - encryption
  - reactive
  - local-storage
  - persistence

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
